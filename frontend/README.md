# Vibe Cart Frontend (React + Vite) with Auth

## Setup
```bash
cd frontend
npm install
npm run dev
```
- Runs on http://localhost:5173
- Proxies `/api` to http://localhost:4000
- Login/Register to get a JWT; cart is user-specific.
